import { DeadlineSortPipe } from './deadline-sort.pipe';

describe('DeadlineSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DeadlineSortPipe();
    expect(pipe).toBeTruthy();
  });
});
