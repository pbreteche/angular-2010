import { ParityDirective } from './parity.directive';

describe('ParityDirective', () => {
  it('should create an instance', () => {
    const directive = new ParityDirective();
    expect(directive).toBeTruthy();
  });
});
